# Smooth Scroll

A small macOS menu bar app that makes mouse-wheel scrolling smooth.

Install locally:

```zsh
git clone https://github.com/5eanxlee/smooth-scroll.git smooth-scroll
cd smooth-scroll
./install.sh
```
